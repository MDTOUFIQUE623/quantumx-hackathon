services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./pq_voting:/app/pq_voting
      - ./static:/app/static
      - ./tests:/app/tests
    environment:
      PQ_VOTING_DATA_DIR: /app/data
